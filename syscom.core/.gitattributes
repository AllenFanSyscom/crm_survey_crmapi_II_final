# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Force the following filetypes to have unix eols, so Windows does not break them
*.* text eol=crlf encoding=utf-8

# These files are text and should be normalized (convert crlf =&gt; lf)
*.cs      text diff=csharp encoding=utf-8
*.xaml    text encoding=utf-8
*.csproj  text encoding=utf-8
*.sln     text encoding=utf-8
*.tt      text
*.ps1     text encoding=utf-8
*.bat     text encoding=utf-8
*.cmd     text
*.msbuild text
*.md      text encoding=utf-8
*.meta    text
*.js      text encoding=utf-8
*.ts      text encoding=utf-8
*.css     text encoding=utf-8
*.htm     text encoding=utf-8
*.cshtml  text encoding=utf-8

# Explicitly declare text files you want to always be normalized and converted
# to native line endings on checkout.
*.c text encoding=utf-8
*.h text encoding=utf-8

# Declare files that will always have CRLF line endings on checkout.
*.sln text eol=crlf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.gif binary
*.psd binary
